
checkout:
    post:
        - git submodule sync
        - git submodule update --init --recursive
        - (cd GlowstonePlusPlus/Glowkit && mvn install)
	- mv GlowstonePlusPlus/src/main/java/net src/main/java
        - mvn package
test:
    post:
        - cp -r Dragonet-core/target/*.jar $CIRCLE_ARTIFACTS/
