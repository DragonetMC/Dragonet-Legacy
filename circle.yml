
checkout:
    post:
        # compile bukkit
        - git clone --recursive git://github.com/DragonetMC/Dragonet.git
        - (cd GlowstonePlusPlus/Glowkit && mvn install)
test:
    post:
        - cp -r Dragonet-core/target/*.jar $CIRCLE_ARTIFACTS/
