
checkout:
    post:
        # compile g++
        - git submodule sync
        - git submodule update --init --recursive
        - (cd GlowstonePlusPlus/Glowkit && mvn install)
        - (cd GlowstonePlusPlus-upstream && mvn install)
        - ls Dragonet/src/main/java
        - mvn install
        - (cd GlowstonePlusPlus && mvn install)
        - mvn package
test:
    post:
        - cp -r Dragonet-core/target/*.jar $CIRCLE_ARTIFACTS/
