
checkout:
    post:
        # compile bukkit
        - (cd Glowkit && mvn install)
test:
    post:
        - cp -r Dragonet-core/target/*.jar $CIRCLE_ARTIFACTS/
# deploy to test server
#deployment:
#  devel:
#    branch: master
#    commands:
#      - scp -P 2299 $CIRCLE_ARTIFACTS/glowstone++-1.8.6-SNAPSHOT.jar slog@therealms.us:glowstone.jar
#      - ssh slog@therealms.us -p 2299 './start.sh'
